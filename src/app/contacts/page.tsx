import React from "react";

const Contacts = () => {
    return (
        <main>
            <section>
                <div className="container">Contacts</div>
            </section>
        </main>
    );
};

export default Contacts;
