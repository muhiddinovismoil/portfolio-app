import { EducationI } from "@/data";
import React from "react";

export const EductaionUi = ({
    id,
    link,
    title,
    endDate,
    subTitle,
    startDate,
}: EducationI) => {
    return <div>EductaionUi</div>;
};
